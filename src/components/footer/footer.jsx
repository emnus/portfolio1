import React from 'react'

function footer (props) {
  return (
    <div>
        <div>
        <a href="">{prop.credits}</a>
        <a href="">{prop.aboutsus}</a>
        </div>
        <div>
            <a href="">{props.linkedin}</a>
            <a href="">{props.whatsapp}</a>
            <a href="">{props.mail}</a>
            
        </div>
    </div>
  )
}

export default footer